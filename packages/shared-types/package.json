{
  "name": "@school-erp/shared-types",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TypeScript interfaces, enums and Zod schemas used by both web and api",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.7.5"
  }
}